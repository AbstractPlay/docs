name: Deploy Docs Dev

on:
  push:
    branches: [develop]
  repository_dispatch:
    types: [dep_update_dev]
  workflow_dispatch:

jobs:
  deploy-dev:
    runs-on: ubuntu-latest
    env:
      DOCS_CF_DIST: ${{ secrets.DOCS_CF_DIST_DEV }}
    steps:
      - uses: actions/checkout@v4
        with:
          ref: develop
          submodules: recursive
          token: ${{ secrets.PAT_READ_PACKAGES }}

      - name: Update submodules to latest branch tip
        run: |
          git submodule sync --recursive
          git submodule update --init --recursive
          for path in vendor/renderer vendor/gameslib; do
            git -C "$path" fetch --depth=1 origin develop
            git -C "$path" checkout FETCH_HEAD
          done

      - uses: actions/setup-node@v4
        with:
          node-version: 18.x
          cache: npm

      - name: Configure npm for GitHub Packages
        run: |
          echo "@abstractplay:registry=https://npm.pkg.github.com/" >> .npmrc
          echo "//npm.pkg.github.com/:_authToken=${{ secrets.PAT_READ_PACKAGES }}" >> .npmrc

      - run: npm ci
      - run: npm run build
        env:
          DOCS_CHECK_WARN: "0"
          DOCS_STAGE: dev

      - name: Configure AWS credentials
        uses: Fooji/create-aws-profile-action@v1
        with:
          profile: AbstractPlayDev
          region: us-east-1
          key: ${{ secrets.AWS_KEY }}
          secret: ${{ secrets.AWS_SECRET }}

      - name: Deploy to S3
        run: npx serverless client deploy --stage dev --no-confirm

      - name: CloudFront invalidation
        if: env.DOCS_CF_DIST != ''
        env:
          AWS_SDK_LOAD_CONFIG: "1"
          AWS_PROFILE: AbstractPlayDev
        run: aws cloudfront create-invalidation --distribution-id "$DOCS_CF_DIST" --paths "/*"
